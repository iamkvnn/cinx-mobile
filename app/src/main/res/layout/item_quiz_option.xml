<?xml version="1.0" encoding="utf-8"?>
<!--
    item_quiz_option.xml
    A single selectable option row in the quiz RecyclerView.
    Background is swapped to indicate: default / selected / correct / wrong.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="5dp">

    <LinearLayout
        android:id="@+id/optionContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_quiz_option"
        android:padding="16dp"
        android:clickable="true"
        android:focusable="true">

        <!-- Radio circle / result icon -->
        <FrameLayout
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginEnd="14dp">

            <!-- Default radio outline -->
            <View
                android:id="@+id/viewRadioDefault"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="@drawable/bg_circle_outline"
                android:layout_gravity="center" />

            <!-- Result icon (hidden by default) -->
            <ImageView
                android:id="@+id/ivOptionResult"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_check_circle"
                android:contentDescription="Result"
                android:visibility="gone" />
        </FrameLayout>

        <!-- Option text -->
        <TextView
            android:id="@+id/tvOptionText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="14sp"
            android:textColor="@color/text_primary"
            android:lineSpacingMultiplier="1.3"
            tools:text="Ctrl + Alt + K / Cmd + Option + K" />

        <!-- Correct/Wrong label (hidden by default) -->
        <TextView
            android:id="@+id/tvOptionLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textStyle="bold"
            android:layout_marginStart="8dp"
            android:visibility="gone"
            tools:text="Chính xác!" />

    </LinearLayout>
</FrameLayout>
