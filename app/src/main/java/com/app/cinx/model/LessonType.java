package com.app.cinx.model;

/**
 * Enum representing the type of a learning lesson.
 * Easily extensible by adding new types (e.g., AUDIO, LIVE_SESSION).
 */
public enum LessonType {
    VIDEO,    // Bài học video
    DOCUMENT, // Tài liệu đọc
    QUIZ      // Trắc nghiệm
}
